# 用户故事

作为一个色彩菜鸡，我经常尴尬的发现，涂完了颜色以后整体很不协调，浪费了我挺满意的线稿。但是已经没法再重新上色，因为第二次重新画一次就找不回当初的热情了。

我想要的是，不浪费画材的情况下，提前预览最终整体效果。当前的绘画软件无法不多不少的实现以下流程，并且没有额外功能侵占用户界面：

## 导入画材

 - 导入色卡照片，自动识别色块及色卡上印刷的色号，转化为电子版的画笔色板。
 - 自动识别结果允许用户手动纠正，包括色号和对应颜色。
 - 色卡文件选择器使用配置的色卡导入默认路径。

## 导入线稿

 - 线稿是可选项；用户可以直接使用空白画布，也可以从图片文件导入线稿。
 - 已导入的线稿可以移除并恢复为空白画布。
 - 提供文件路径输入框，并预设配置的线稿导入默认路径。
 - 路径输入支持类似命令行的自动补全，且匹配时不区分大小写。

## 画布样板

 - 样板用于选择画布比例，不表示纸张材质。
 - 提供正方形（1:1）、竖向长方形（3:4）和横向长方形（4:3）三种画布样板。
 - 导入的线稿保持原始比例，并在所选画布内居中适配。

## 软件内操作

 - 选择不同色号
 - 画布支持放大和缩小。
 - 默认缩放比例应让整个画布完整显示在可用区域内。
 - 鼠标或者压感笔上色，但是并不需要压感，只是提供笔操作方便而已。
 - 使用普通画笔上色，不提供油漆桶填充。
 - 替换上色，比如用C1上色的部分全部替换为C2。不需要重新涂色，直接替换
 - 在画的边上显示所有该画使用了的色号，如果能有个箭头对应上最好，不过MVP就不需要了。这个地方可以用来切换色号。

## 生成色号参考

 - “软件内操作”里关于色号显示部分有重复，就不再赘述了。如果有额外需求就写在这。有一种可能是，最后点击“生成参考”的时候再加上上色区域和色号之间的连线，方便查看。
 - 导出格式为 PNG。
 - 导出图片尺寸与画面区域尺寸相同。
 - 导出位置默认使用配置的完成图导出路径。

## 本地配置

 - 所有本地设置统一保存在 `~/.config/my_painter_shop/config.yaml`。
 - 配置文件包含以下三个默认路径：
   - 色卡文件导入路径。
   - 线稿图片导入路径。
   - 完成图导出路径。

## 本地部署

 - 软件作为本地 Web 应用运行，仅监听本机地址。
 - 默认访问地址为 `http://127.0.0.1:7006`。
